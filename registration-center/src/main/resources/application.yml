spring:
  application:
    name: registeration-center-single
eureka:
  client:
    service-url:
      defaultZone: http://registeration-center:8000/eureka/
    fetch-registry: false
    register-with-eureka: false
server:
  port: 8000

